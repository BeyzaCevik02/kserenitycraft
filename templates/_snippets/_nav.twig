{% set navigation = craft.navigate.raw('mainNav') %}

<ul>
    {% for item in navigation %}
        <li class="{{ item.url == craft.app.request.url ? 'active' : '' }}">
            <a href="{{ item.url }}">
                {{ item.name }}
            </a>
        </li>
    {% endfor %}
</ul>
